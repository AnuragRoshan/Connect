/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.ctfassets.net', '1000logos.net', 'seeklogo.com', 'plus.unsplash.com', 'images.unsplash.com'], // Add other external domains as needed
    },
};

export default nextConfig;
